<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.entregavelrestaurante">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activitys.DetalhePratoActivity"></activity>
        <activity android:name=".views.activitys.DetalheRestauranteActivity" />
        <activity android:name=".views.activitys.TelaDeLoginActivity" />
        <activity android:name=".views.activitys.TelaDePerfilActivity" />
        <activity android:name=".views.activitys.TelaDeCadastroActivity" />
        <activity android:name=".views.activitys.MainActivity" />
        <activity android:name=".views.activitys.TelaDeSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>